{
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "rimraf dist && babel lib -d dist",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "copy-dir": "0.0.8",
    "tempfile": "1.1.1"
  },
  "devDependencies": {
    "babel-cli": "6.7.5",
    "babel-preset-es2015": "6.6.0",
    "rimraf": "2.5.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
